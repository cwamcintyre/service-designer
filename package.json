{
  "name": "service-designer",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*/*"
  ], 
  "scripts": {
    "turbo": "turbo",
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}